count = 0
n = int(input("Enter starting number:"))
while n > 1:
 if n % 2 == 0:
   print(n / 2)
   n  = n / 2
 elif n % 2 != 0:
  print((n * 3) + 1)
  n = (n * 3) + 1
 count = count + 1
 if n == 1:
  break
print("steps:",count)
